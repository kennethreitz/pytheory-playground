[project]
name = "pytheory-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytheory>=0.54.1",
    "responder>=3.12.0",
]
